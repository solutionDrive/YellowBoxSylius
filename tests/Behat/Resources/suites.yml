# Put your Behat suites definitions here

default:
    suites:
        yellowbox:
            contexts_services:
                - sylius.behat.context.hook.doctrine_orm

                - sylius.behat.context.setup.channel
                - solutiondrive.context.ui.shop.yellowbox

            filters:
                tags: "@ui && @yellowbox"
